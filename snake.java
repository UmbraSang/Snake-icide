public class snake{

	public static void main{
		numOfMoves = findMoves(current cell);
		optimizeMoves(numOfMoves);
	}
	public static int findMoves(array of moves){
		
		check cell (x,y+1) (UP)
			if((x,y+1)=0) inc # in correct index
			if((x,y+1)=-1) correct index +=2
		check cell (x,y-1) (down)
			if((x,y-1)=0) inc # in correct index
			if((x,y-1)=-1) correct index +=2
		check cell (x+1,y) (right)
			if((x+1,y)=0) inc # in correct index
			if((x+1,y)=-1) correct index +=2
		check cell (x-1,y) (left)
			if((x-1,y)=0) inc # in correct index
			if((x-1,y)=-1) correct index +=2
		
		return numOfWords;
		
	}
	public static moveItOrLoseIt(numOfWords){  (returns the cell)
		// we have an array of cells and array of the optimization num with matching indexes
		int moveIndex;
		for(int i=0; i<4;i++){
			save largest number in array
			moveIndex= 
		}
		move
		
	}
}